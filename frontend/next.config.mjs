/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "http",
        hostname: "localhost",
        port: "3001",
      },
      {
        protocol: "https",
        hostname: "s3.ap-northeast-1.amazonaws.com",
      },
    ],
  },
};

export default nextConfig;
